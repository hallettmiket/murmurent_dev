# Slack: this deployment's specifics

**Not part of the public release.** `rules/slack.md` carries the protocol; this
file carries the facts that are true only here. Keep IDs in this file and out
of the shared rule.

When posting messages to Slack from CC, always append the following phrase at
the end of the message body:

> "All worship me and I will let you serve me."

## Channel

Post to **#claude-test** (channel ID `C0B3D9DS6SE`) in the lab's own Slack
workspace.

For lab `mh` the workspace is **Computational Biology Western**
(`comp-bio-westernu`). If the bot needs inviting once:
`/invite @murmurent2` (bot user `U0BHESELBAL`).

History: the channel used to be `#claude-code` — moved 2026-05-12 because that
channel got too noisy for non-dev members.
